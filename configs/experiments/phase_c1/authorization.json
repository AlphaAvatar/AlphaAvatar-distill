{
 "schema": "aadistill.experiment_authorization_config/v1",
 "_contract": "The Phase-C1 INSTANCE data the authorization assembler needs: which committed objects to read, which money figures to refuse a mis-priced session against, and what the session's stage conditions say. Every value here was a literal inside src/aadistill until 2026-09-08. Moving it out is what lets the core be reused by another experiment. AUTHORIZES NOTHING: this is configuration, not a grant.",
 "experiment_id": "phase_c1",
 "stage_id": "1",
 "_stage_id_meaning": "Which PIPELINE STAGE this experiment belongs to, DECLARED by the maintainer rather than inferred from a name or from which code the pod happens to execute. C1 is Stage 1 -- projection and structural initialization. Its two arms are two Stage-1 initializations that differ in exactly one operator of DEPTH->FFN->RESIDUAL_WIDTH->ATTENTION, and the question it answers is which ATTENTION initialization to keep. The six 0.86M recovery probes it trains are the measuring INSTRUMENT for that question, not the subject: grouping the runs by the instrument would file an initialization experiment next to Stage-3 recovery work and lose what it is for. The PHASE is C, carried by experiment_id 'phase_c1'; the ATTEMPT is the run_id. Stage, phase and attempt are three dimensions and this field is only the first. Consumed by scripts/experiments/run_layout.py to place runs at logs/runs/stage-1/phase_c1/<run_id>/.",
 "authorization_id": "autoinit.v1.phase_c1",
 "plan_id": "autoinit.v1.phase_c1",
 "inputs": {
  "preregistration": "logs/phase_c1_execution_preregistration.json",
  "pricing": "logs/phase_c1_pricing.json",
  "battery_identity": "logs/phase_c1_battery.json",
  "teacher_binding": "logs/phase_c1_teacher_binding.json",
  "scoring_equivalence": "logs/phase_c1_scoring_equivalence.json"
 },
 "output": "logs/autoinit_c1_authorization.json",
 "grant_contract": {
  "contract_id": "phase_c1.grant/v1",
  "stated_fields": [
   "granted_by",
   "covers",
   "cumulative_spend_at_approval_usd",
   "cumulative_cap_usd",
   "does_not_authorize"
  ],
  "derived_fields": [
   "granted_utc",
   "authorized_session_commit",
   "harness_source_digest",
   "plan_hash",
   "science_plan_hash",
   "preregistration_sha256",
   "battery_content_sha256",
   "scoring_contract_digest",
   "teacher_revision"
  ]
 },
 "accepted_pricing": {
  "_why_restated": "so a mis-priced session is REFUSED rather than mirrored. A ceiling that only echoes its source is not an independent check, and this is the last place a mis-priced session can be stopped before a grant exists. Amended 2026-09-04 for the approved repricing to the secure L40S rate of $1.09/h; every minute assumption is unchanged. The superseded triple at $0.99/h was 13.7578 / 12.2070 / 13.4277.",
  "hard_ceiling_usd": 15.1475,
  "planning_floor_usd": 13.4401,
  "soft_stop_usd": 14.7841,
  "cumulative_cap_usd": 320.0,
  "_cap_amendment": "2026-09-11: the PROJECT cumulative cap rose from 283.76 to 320.00 (+36.24) with the approved execution package below. The per-attempt ceiling did NOT move and no minute assumption changed. The issuer refuses a grant naming any other cap, which is why this value lives here rather than being inferred from the ledger."
 },
 "execution_package": {
  "_contract": "The maintainer decision this experiment's attempts run under. INSTANCE POLICY, which is why it is configuration and not core: the core knows nothing about attempts, packages, dollars or retries. AUTHORIZES NOTHING BY ITSELF -- a launch still needs a grant, an authorization, a launch-bound readiness record and a bundle.",
  "package_id": "phase_c1.execution_package.2026-09-11",
  "approved_utc": "2026-09-11",
  "approved_by": "maintainer decision, 2026-09-11, after independent engineering review of 55bb324a..199711af",
  "formal_attempts_max": null,
  "formal_attempts_include_the_first": true,
  "per_attempt_hard_ceiling_usd": 15.1475,
  "formal_allowance_usd": 45.4425,
  "gpu_engineering_allowance_usd": 6.0,
  "package_total_usd": 51.4425,
  "cumulative_spend_at_approval_usd": 267.8998,
  "reconciliation_margin_usd": 0.6577,
  "limits_do_not_transfer": "the engineering allowance, the per-session ceiling, the package total and the project cap bind SEPARATELY. An unspent engineering allowance does not raise the per-session ceiling, and the $0.6577 margin is for real accounting corrections, not spending. The attempt COUNT is no longer one of these limits -- see _attempts_amendment_2026_09_11b -- but the money is unchanged, and money is what now bounds how many sessions are possible.",
  "attempt_counting": {
   "rule": "invoking the formal launcher under a NEW one-use authorization is one formal SESSION. Sessions are still numbered, recorded and ledgered individually; what was withdrawn on 2026-09-11 is the CAP on how many there may be, not the counting.",
   "not_counted": "read-only checks, code repair and GPU engineering validation before an authorization is issued.",
   "prospective_only": "this governs THIS package. It does not rewrite the 2026-09-04 ruling, under which attempt 3 was not consumed because no provider resource was created, nor the counting attempts 10 and 11 ran under.",
   "no_hidden_retries": "one provider-create call per DRAW, at most three draws per session, at most one billing resource at any instant, and every draw recorded with its own pod id, cost and watchdog. A redraw is not a hidden retry; an unrecorded one would be."
  },
  "autonomous_formal_retry_requires_all_of": [
   "it can be CONFIRMED that no formal probe training has started",
   "the failure is an ordinary infrastructure, setup or $0 pre-provider failure, and its cause is identified and handled -- for a provider transient, backing off and requesting another host IS the handling, and no repository change is required",
   "a deterministic engineering error is not repeated unchanged: locate, fix minimally, verify, update the affected bindings, then retry",
   "frozen science, the input contracts and the decision rule are unchanged",
   "the previous resource is confirmed no longer billing",
   "the remaining package balance funds the planned session plus evidence recovery and teardown",
   "the new session uses a new run identity and a complete, valid one-use authorization chain"
  ],
  "stop_and_report": [
   "formal probe training has STARTED and a failure follows -- peripheral engineering may be repaired and the SAME experiment resumed only where the existing protocol already supports it and state, identity and continuity verify; resume semantics are never invented, and model weights alone are not assumed sufficient",
   "it cannot be confirmed whether training started -- absence of confirmation is not confirmation of absence",
   "a real replay mismatch, a frozen-science conflict, or evidence whose provenance cannot be verified",
   "any complete, valid GO / NO-GO / INCONCLUSIVE -- all three END this round's formal execution, and re-running a valid result for a better one is forbidden",
   "budget, resource scope or a rerun that cannot preserve scientific validity"
  ],
  "never": [
   "re-running a valid INCONCLUSIVE in pursuit of a GO",
   "splicing probes across attempts",
   "substituting a seed",
   "selectively retaining outputs",
   "changing arms, seeds, recipe, battery, statistics or the behavioural vetoes"
  ],
  "engineering_allowance_does_not_authorize": [
   "complete formal probes",
   "consuming the frozen confirmation battery for an endpoint",
   "formal execution -- that comes from the $45.4425 C1 formal allowance, and the two do not transfer"
  ],
  "_attempts_amendment_2026_09_11b": "The three-attempt cap and the one-provider-resource-per-invocation rule are WITHDRAWN, prospectively, by a maintainer decision of 2026-09-11 taken after attempt 11. Ordinary infrastructure failures, setup failures and $0 pre-provider refusals -- where it is CONFIRMED that no formal probe training has started -- are now diagnosed, handled and retried autonomously inside the remaining budget, with no package-level cap on the number of launcher invocations. Attempts 10 and 11 keep their records, costs, numbering and the authorization semantics they actually ran under; what is withdrawn is a future constraint, not a record.",
  "what_still_binds": [
   "the money: $45.4425 C1 formal, $6.0000 engineering, $51.4425 package, $320.0000 project cap",
   "$15.1475 per launcher SESSION, covering every host draw inside it -- draws do not each get a ceiling",
   "at most ONE BILLING resource at any instant, now enforced by SessionRunner.release_and_confirm rather than by making the redraw branch unreachable",
   "secure L40S at no more than $1.09/h; no other GPU, no price chasing",
   "every cost counts: a failure, a new run_id, a new pod or a new authorization resets nothing",
   "the science: arms, seeds, recipe, battery, statistic and behavioural vetoes are frozen"
  ],
  "acquisition": {
   "host_draws_max": 3,
   "what_a_draw_is": "replacing a host that never became usable -- cold, or no endpoint -- inside ONE session and inside its one ceiling. A draw is not an attempt and not a way to chase stock.",
   "create_attempts": 1,
   "why_create_attempts_stays_one": "a create-attempt sleeps and asks the same market again. Draws replace an unusable host; that is the authorized mechanism, price chasing is not.",
   "between_batches": "after a batch is exhausted, inspect the failure mode and supply before continuing. Low stock is a reason to back off at $0 and pick a better moment, not a proven cause -- one Medium success and one Low failure is not a finding, and nothing about it is written into src/aadistill.",
   "release_rule": "a returncode, a null runtime, a failed query or a cleared local pod_id are NOT evidence that a resource stopped billing. Only the provider saying so is. If it cannot be confirmed, no further resource is created and the state is reconciled first."
  },
  "success_is": "one complete, valid, fully evidenced C1 result. GO is not required: a valid NO-GO or INCONCLUSIVE ends the round just as much."
 },
 "authorized_stages": [
  0,
  1,
  2,
  3,
  4,
  5
 ],
 "stage_conditions": {
  "replay": "stop before any recovery training on a parent or incumbent digest mismatch; preserve the evidence and treat it as the attempt's result",
  "device_handoff": "stop before recovery if the card is not released",
  "training": "all six trainings must complete before any evaluation; a training failure is C1_INCOMPLETE, not a partial confirmation experiment",
  "generation_admission": "no probe is scored unless the generation protocol observed from its own raw summaries is comparable to the attested C1 protocol",
  "decision": "stage I requires six complete admitted and scored probes",
  "retry": "THIS authorization funds exactly ONE launcher session. Inside that session the launcher may take up to the approved number of acquisition draws, replacing a host that was created and never became usable; at most ONE resource may be billing at any instant, enforced by SessionRunner.release_and_confirm, and every draw shares this session's single ceiling. A FURTHER session needs a new grant, a new launch-bound readiness record, a new authorization and a new bundle -- see execution_package for when that is permitted without a further maintainer decision."
 },
 "one_use": "ONE grant, ONE issuance, ONE launcher session. Within the session, acquisition draws are bounded by execution_package.acquisition and never exceed one BILLING resource at a time. A failure is this session's result; a new session always uses a complete new one-use chain and never reuses a consumed one.",
 "authorizes": "nothing",
 "_superseded_wording_note": "Until 2026-09-11 these two fields read 'no replacement pod' and 'ONE provider resource'. That was the rule attempts 1-12 ran under and it is preserved in their issued authorizations, which are not rewritten. It stopped describing the generation source when the package permitted bounded acquisition draws, and an authorization that contradicts the package it is issued under is a governance defect even when nothing reads the prose."
}
