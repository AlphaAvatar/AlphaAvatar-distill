# governance/

what permitted this run: the maintainer grant, the one-use authorization, the readiness record and the bundle record. INPUTS and one-use snapshots, not products.

Roles this run declares here: `authorization`, `bundle_record`, `grant`, `readiness_record`.

Described by `../manifest.json`, which is authoritative.
