# artifacts/

what it produced or brought home. Large objects stay in the session scratch and are referenced by hash from `artifacts/manifest.json`; this directory holds reviewable text.

Roles this run declares here: `artifacts`.

Described by `../manifest.json`, which is authoritative.
