# runtime/

how it executed: the session record the runner writes on every path, the launcher console, the watchdog journal.

Roles this run declares here: `launcher_log`, `session_record`, `watchdog_journal`.

Described by `../manifest.json`, which is authoritative.
