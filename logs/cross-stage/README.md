# logs/cross-stage

Experiments whose pipeline stage no frozen record establishes. That is a finding, not a holding pen: an identifier names the experiment, and a driver stage or an operator id is a different dimension. When a record does establish a stage, the experiment moves under it.

| directory | material |
| --- | --- |
| [`continuation_b/`](continuation_b/) | analyses, plans, runs |
| [`early/`](early/) | analyses, history |
| [`measurement/`](measurement/) | analyses, runs |
| [`phase_a/`](phase_a/) | analyses, history, plans, results, runs |
| [`phase_b/`](phase_b/) | analyses, plans, runs |
| [`recovery_continuation/`](recovery_continuation/) | analyses, history, runs |

Canonical run list: [`../index.json`](../index.json).
